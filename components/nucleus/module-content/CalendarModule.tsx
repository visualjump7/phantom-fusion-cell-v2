"use client";

/**
 * Phase 7 replaces this stub with the ICS-integrated calendar module
 * (shared calendars + system events from bills/decisions/travel).
 */

import { UnderConstructionStub } from "./UnderConstructionStub";

export function CalendarModule() {
  return <UnderConstructionStub label="Calendar" />;
}

export default CalendarModule;
