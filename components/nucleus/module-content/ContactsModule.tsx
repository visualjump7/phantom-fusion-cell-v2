"use client";

/**
 * Phase 6 replaces this stub with the full global contacts directory.
 */

import { UnderConstructionStub } from "./UnderConstructionStub";

export function ContactsModule() {
  return <UnderConstructionStub label="Contacts" />;
}

export default ContactsModule;
